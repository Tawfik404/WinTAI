{
  "name": "wintai",
  "version": "0.1.0",
  "description": "A fully local Windows AI assistant — monorepo root.",
  "private": true,
  "scripts": {
    "dev:frontend": "cd apps/frontend && npm run dev",
    "dev:backend": "cd apps/backend && python -m app.main",
    "build:frontend": "cd apps/frontend && npm run build",
    "setup:frontend": "cd apps/frontend && npm install",
    "setup:backend": "cd apps/backend && python -m venv .venv && .venv/Scripts/pip install -r requirements.txt"
  }
}
